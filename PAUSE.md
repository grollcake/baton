# Pause

이 문서도 [`UNINSTALL.md`](UNINSTALL.md)와 같은 독자를 위한 것입니다. 아직
설치하지 않고 이 저장소를 웹에서 읽으며 도입 여부를 판단하는 사람이라
`.baton/`을 볼 수 없습니다. 절차는 여기 없습니다 -- 설치 후에는
`.baton/HOW-TO-UPDATE.md`와 `<baton> guide pause`에 있습니다.

`pause`는 되돌릴 수 있고, 아무것도 지우지 않습니다. 규칙 블록이 "Baton은
중지됨"으로 바뀔 뿐이라, 에이전트는 지시 파일을 읽고 Baton이 설치되지 않은
것처럼 행동합니다. 기록과 설치 파일은 그대로 남습니다. `resume`은 그 블록을
원래대로 정확히 되돌립니다.

중지 중에는 기록을 남기거나 라운드를 진행하는 명령이 거부되고 아무것도
바꾸지 않습니다. 상태만 보는 명령은 그대로 동작하며 중지 상태임을 알립니다.

두 가지는 미리 알아 둘 만합니다. 열린 작업이 있으면 `pause`는 거부되며,
그래도 중지하려면 강제해야 합니다. 그리고 중지된 프로젝트는 업데이트 전에
반드시 `resume`해야 합니다 -- 업데이트가 규칙 블록을 다시 쓰면서 중지를
조용히 풀어 버리는 것을 막기 위해서입니다.
