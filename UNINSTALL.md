# Uninstall

이 문서는 **AI 에이전트가 사용자 프로젝트에서 Baton을 뺄 때 따라야 하는 절차**입니다.
사람이 직접 따라할 수도 있지만, 기본은 에이전트가 이 문서를 읽고 수행합니다.

설치 절차는 [`BOOTSTRAP.md`](BOOTSTRAP.md), 잠시 멈추는 방법은 아래 "제거 대신
중지"를 참조하세요.

---

## 먼저: 제거가 맞는지 확인한다

사용자가 "Baton 그만 쓰자"고 할 때 원하는 것이 제거가 아닌 경우가 많습니다.

| 사용자가 원하는 것 | 해야 할 일 |
| --- | --- |
| 당분간 안 쓰고 싶다 | `<baton> pause`. 기록도 설치 파일도 그대로 두고 규칙만 끈다 |
| 이 작업만 규약 없이 하고 싶다 | 아무것도 하지 않는다. 짧은 질의응답과 설명은 원래 기록 대상이 아니다 |
| 이 저장소에서 완전히 빼고 싶다 | 이 문서를 따른다 |

`pause`는 되돌릴 수 있고 제거는 되돌릴 수 없습니다. 확실하지 않으면 사용자에게
묻고, 답이 모호하면 `pause`를 먼저 제안합니다.

---

## 제거 절차

### 1. 무엇이 지워지는지 먼저 보여 준다

```text
<baton> remove
```

`--apply` 없이 실행하면 아무것도 바꾸지 않고 계획만 출력합니다. 이 출력을
사용자에게 그대로 보여 주고 승인을 받습니다. 에이전트가 대신 판단하지 않습니다.

출력에는 지울 파일, 남길 파일, Baton이 자기 것으로 알아보지 못해 남기는 파일,
그리고 열린 작업이 있으면 그 목록이 들어 있습니다.

### 2. 승인을 받은 뒤 실행한다

```text
<baton> remove --apply
```

되돌릴 수 없습니다. Git 저장소라면 실행 전에 커밋되지 않은 변경이 없는지
확인하는 편이 안전합니다.

### 3. 결과를 확인한다

`.baton/`에 기록이 남아 있고 `AGENTS.md`와 `CLAUDE.md`에 `<baton-rules>` 블록이
없으면 제거된 상태입니다. `<baton> lint`는 제거된 프로젝트에서도 동작하며,
남은 기록이 온전한지만 검사합니다.

지시 파일에 프로젝트 자신의 내용이 없고 Baton이 파일 전체를 썼던 경우에는 블록만
빠지는 것이 아니라 **파일이 삭제**됩니다. dry run 출력에
`deleted (Baton wrote the whole file)`로 표시되므로, 그 파일을 계속 쓸 생각이라면
승인 전에 사용자에게 알립니다.

---

## 거부되는 경우와 대처

`remove`는 되돌릴 수 없는 손실이 생길 수 있으면 거부합니다. 거부를 우회하기 전에
왜 거부했는지 사용자에게 전달합니다.

| 거부 | 뜻 | 대처 |
| --- | --- | --- |
| 열린 작업이 N개 | 진행 중인 작업이 기록만 남고 버려진다 | 작업을 닫거나, 버리기로 했다면 `--force` |
| 블록이 활성본과도 중지본과도 다르다 | 마커 안에 사람이 쓴 글이 있다 | 그 글을 마커 **밖으로** 옮기고 다시 실행한다. `--force`로는 통과하지 못한다 |
| `--purge` 거부: 커밋되지 않았다 | 지우려는 기록이 Git 히스토리에 없다 | 먼저 커밋한다 |
| `--purge` 거부: Git 저장소가 아니다 | 지우면 복구할 방법이 없다 | 백업을 직접 만든 뒤 수동으로 지운다 |

마커 안 사용자 텍스트에 `--force`가 없는 것은 의도된 설계입니다. 그 바이트는
Baton이 배포본에서 복원할 수 없습니다.

---

## 기록을 함께 지울 때

```text
<baton> remove --apply --purge
```

`--purge`는 `BATON-LOG.txt`, `runs/`, `GUIDANCE.md`, `LESSON-LEARNED.md`,
`lesson-learned/`까지 지웁니다. **기본값이 아닌 이유**는 이 파일들이 Baton의
산물이 아니라 프로젝트가 만든 기록이기 때문입니다. `runs/`는 트리에 남아 있는
코드가 왜 그렇게 되었는지 설명하는 유일한 문서인 경우가 많습니다.

`--purge`는 `.baton/` 아래 Git이 무시하지 않는 모든 경로가 추적되고 깨끗할 때만
통과합니다. 확인 대화가 아니라 이 조건으로 지키는 이유는, 에이전트 파이프라인에서
확인 대화는 진행하려던 에이전트가 답하기 때문입니다. 조건이 충족되면 지워진
내용은 `git show`로 되살릴 수 있습니다.

`--purge`를 제안하기 전에 사용자에게 무엇이 사라지는지 구체적으로 말합니다.
타임라인 몇 줄, 작업 기록 몇 건인지는 `remove` 출력에 있습니다.

---

## 제거한 뒤

- **`baton` 명령은 더 이상 동작하지 않습니다.** 바이너리가 지워졌습니다.
- **`resume`으로 돌아올 수 없습니다.** 중지가 아니라 제거이기 때문입니다.
- **다시 설치하려면** [`BOOTSTRAP.md`](BOOTSTRAP.md)를 따릅니다. 다만 부트스트랩
  1단계는 `.baton/`이 이미 있으면 중단하므로, 기록을 남긴 채 재설치하려면
  사용자에게 확인하고 그 범위로 한정해 진행합니다.
- **남은 기록은 읽을 수 있습니다.** `BATON-LOG.txt`와 `runs/`는 평문이라 Baton
  없이도 읽힙니다.

---

## 제거 대신 중지

```text
<baton> pause      # 규칙만 끈다
<baton> resume     # 되돌린다
```

중지 중에는 기록을 쓰거나 라운드를 진행하는 명령이 거부되고, 상태를 보는 명령은
동작하면서 중지 상태임을 알립니다. 기록과 설치 파일은 건드리지 않습니다.

중지된 프로젝트는 업데이트 전에 `resume`이 필요합니다. 업데이트가 규칙 블록을
다시 쓰면서 중지를 조용히 푸는 것을 막기 위해서입니다.
