# Uninstall

이 문서는 **Baton을 도입할지 판단하는 사람**을 위한 것입니다. 이 저장소를
웹에서 읽는 시점이라 아직 아무것도 설치하지 않았고, 그래서 `.baton/`도 볼 수
없습니다. 설치 절차가 [`BOOTSTRAP.md`](BOOTSTRAP.md)로 저장소 루트에 있는
것과 같은 이유로, 빠져나가는 길도 루트에 있어야 합니다.

구체적인 절차는 여기 없습니다. 그 절차는 설치된 프로젝트가 실제로 가진 문서,
`HOW-TO-UPDATE.md` 하나에만 있습니다. 두 문서가 같은 절차를 각자 설명하면
결국 어긋나기 때문입니다. 설치한 뒤에는 그 문서를 읽거나
`<baton> guide remove`로 같은 내용을 출력해서 보면 됩니다.

## 제거 전에: 정말 제거가 맞는지

당분간 안 쓰고 싶을 뿐이라면 이 문서가 아니라 [`PAUSE.md`](PAUSE.md)입니다.
되돌릴 수 없는 것은 `remove`뿐입니다. Baton에 "그만 쓰자"고 말하는 사용자는
대체로 되돌릴 수 있는 쪽을 원하는 것이지 완전한 제거를 원하는 것이 아니므로,
에이전트는 둘을 구분해 확인합니다.

## 제거하면 사라지는 것

Baton이 설치한 것들입니다 -- 규약 문서(`PROTOCOL.md`, 역할 파일 셋,
`HOW-TO-UPDATE.md`), 템플릿, `VERSION`, 그리고 `bin/`의 바이너리. 지운 뒤에는
`baton` 명령을 쓸 수 없습니다.

`AGENTS.md`와 `CLAUDE.md`에서는 `<baton-rules>` 블록이 빠집니다. 다만 그 파일에
프로젝트 자신의 내용이 없고 Baton이 전체를 썼던 경우에는 블록만 빠지는 것이
아니라 **파일이 통째로 삭제**됩니다. 어느 쪽인지는 `--apply` 없이 실행하면
미리 알려 줍니다.

Baton이 자기 것으로 알아보지 못하는 파일은 손대지 않고 남기며, 무엇을 남겼는지
보고합니다.

## 제거해도 남는 것

`remove`는 기본적으로 `.baton/`의 기록(`BATON-LOG.txt`, `runs/`,
`GUIDANCE.md`, `LESSON-LEARNED.md`, `lesson-learned/`)을 지우지 않습니다.
이 파일들은 Baton이 만든 산출물이 아니라 프로젝트 자신의 기록이기 때문입니다.
`runs/`는 트리에 남은 코드가 왜 그렇게 되었는지 설명하는 유일한 문서인 경우가
많습니다. 이 기록까지 지우려면 `--purge`를 따로 지정해야 하고, Git으로
커밋되어 있지 않으면 거부됩니다.

## 더 알아보려면

- [`bootstrap/.baton/HOW-TO-UPDATE.md`](bootstrap/.baton/HOW-TO-UPDATE.md) —
  업데이트·중지·제거를 실제로 수행하는 절차. 설치하면 같은 문서가 `.baton/`에
  놓입니다
- `<baton> guide remove` (또는 `pause`, `lifecycle`) — 같은 문서를 설치된
  바이너리에서 바로 출력
