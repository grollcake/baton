# REVIEW-02: Handoff record for a session that must stop

Task ID: eidq
Date: 2026-09-25
Planner: Planner (Claude Code)
Run: .baton/runs/20260925-1130-handoff-RUN-02.md
Status: complete

## Decision

Result: ready-for-user-decision

This review is evidence for the user's decision, not approval. Only the user
can approve `CLOSE`.

## Findings

### Blockers

- none. The feedback is answered: `## Delegates` is stamped by the binary from
  `BATON-LOG.txt` and the artifacts on disk, hand-written content there is
  refused, and the three REVIEW-01 nits are closed. The extracted helpers
  changed no existing behaviour, which was tested rather than assumed.

### Nits

- The seal is not tamper-evident after the fact. Sealing cannot be forged --
  a hand-written body is refused and a body carrying the marker is overwritten
  wholesale -- but editing `.baton/HANDOFF.md` after it is sealed is undetected:
  I changed a stamped delegate line to `- zzzz FORGED` post-seal and `status`
  still printed `handoff: ... | read it before acting`. The same applies to
  `Date:`, and (REVIEW-01 nit 3) to `Log Line:`. A digest line stamped over the
  file would close all three at once. This is a design decision for the user,
  not a defect in the round.
- `revert-check internal/baton/events.go` now reports `pins=none`: the revert
  fails to build (`a.nextGateDescription`, `a.expectedArtifact` undefined), so
  no named test pins it, where in round 01 it pinned three status tests. I
  mutation-tested the extraction instead: an off-by-one in `expectedArtifact`'s
  round arithmetic is caught by four tests, but changing the FEEDBACK next-gate
  text from `EXECUTED (resume Executor work)` to anything else leaves the whole
  suite green. That string was unasserted before this round too, so this is a
  pre-existing gap, not a regression -- but it now also feeds the stamped
  Delegates line, so it is worth one assertion some round.
- `.baton/bin/baton lint` fails on the working tree right now:
  `baton binary checksum failed: checksum mismatch for darwin-arm64/baton`. The
  cause is the `codesign -f -s -` workaround for the exit-137 condition, which
  rewrites the installed binary's bytes; `bootstrap/.baton/bin/darwin-arm64/baton`
  still matches its own checksum, and `.baton/bin/` is gitignored, so nothing
  committed is wrong and nothing in this diff caused it. Per Director this is
  the known `build-release` issue; lint should be re-run once that is fixed.
- `delegateArtifactState`'s `incomplete` bucket is still untested, as RUN-02
  says. I exercised it by hand (a RUN file with a TODO reported `incomplete`, a
  `Status: checkpoint` file reported `checkpoint`), so it works; only the guard
  is missing.

## Suggested User Checks

- Copy `.baton/templates/handoff.md` to `.baton/HANDOFF.md`, fill only Branch
  Strategy, Unrecorded Decisions and Next Intent, and run
  `.baton/bin/baton handoff`. The Delegates section must come back stamped with
  the `[baton-generated: ...]` marker and one line per open task. Compare that
  line against `.baton/bin/baton status --open` and judge whether it tells a
  fresh session what it needs about the delegate that died.
- Hand-write something under `## Delegates` and seal again: it must refuse with
  "written by 'baton handoff', not by hand" and leave the file byte-identical.
- Now edit a line inside an already-sealed record and run
  `.baton/bin/baton status`: it still reports the record as live. Decide whether
  you want the seal to be tamper-evident (a stamped digest) or whether refusing
  at seal time is enough.
- Run `.baton/bin/baton status` and `.baton/bin/baton status --open` on your own
  log and confirm nothing changed except the added handoff line -- the
  `next_gate`, `next_command` and `pending_artifact` lines are the ones the
  refactor moved.
- Read the added paragraph in `.baton/DIRECTOR.md` and confirm it reads as a
  Director rule ("seal only when the user asks"; "the binary writes Delegates"),
  not an exception clause, and that `PROTOCOL.md`, `PLANNER.md` and
  `EXECUTOR.md` are still untouched.

## Evidence Reviewed

- `.baton/runs/20260925-1130-handoff-RUN-02.md` against the PLAN and REVIEW-01;
  diffs to `internal/baton/handoff.go`, `events.go`, `remove.go`,
  `remove_test.go`, `handoff_test.go`, `.baton/DIRECTOR.md`, and the handoff
  template. Both mirrors are byte-identical to their `bootstrap/` copies,
  `baton guide director` matches the installed `.baton/DIRECTOR.md`, and all
  three VERSION files read `0.46.0`, as does the binary.
- On a copy of the tree: `go test ./...` -> `ok`. `baton lint` -> one error, the
  checksum nit above; every other check OK.
- Behaviour preservation, the round's main risk, tested directly: a binary built
  from `HEAD` and one built from this tree produce byte-identical `status`
  output (ignoring the added handoff line) for all 31 task ids in
  `BATON-LOG.txt`, for `status --open`, and for plain `status`; and for eight
  synthetic pipeline states -- REQUEST with and without a path, PLANNED,
  EXECUTED at round 02, REVIEW ready, REVIEW with blockers, REVIEW with a
  missing artifact, and FEEDBACK after RUN-03.
- Mutation test of the extraction: `round+1` in `expectedArtifact` fails
  `TestStatusNextCommand`, `TestHandoffStampsDelegatesForOpenTask`,
  `TestStatusAfterReviewBlockersOffersNextExecutedRound` and
  `TestStatusAfterMissingReviewArtifactOffersNextExecutedRound`; changing the
  FEEDBACK next-gate string fails nothing.
- Forgery probe of the stamped section: template placeholder -> stamped;
  hand-written body -> refused, file byte-identical; body carrying the marker
  plus a fake `- zzzz` task -> accepted but entirely overwritten by the real
  stamp, so no forged line survives a seal; edit after sealing -> survives
  undetected (nit 1).
- Artifact states observed live: `missing`, `incomplete`, `checkpoint`.
- Revert-check on the copy: `internal/baton/remove.go` pins
  `TestRemoveKeepsRecordsByteIdentical` and `TestRemoveReportsSealedHandoffAsKept`;
  `internal/baton/events.go` and `internal/baton/handoff.go` fail the build, so
  neither names a test (nit 2).

## Report To Director

- Suggested summary: RUN-02 verified; Delegates cannot be forged at seal time, status behaviour proven unchanged against a HEAD build, no blockers, four nits
- Artifact path: .baton/runs/20260925-1130-handoff-REVIEW-02.md

## Required Next Step

- ask Director to request user approval
