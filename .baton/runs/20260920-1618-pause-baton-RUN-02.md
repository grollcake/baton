# RUN-02: Let a project pause Baton without removing it

Task ID: srkz
Date: 2026-09-20
Executor: Claude Sonnet 5
Plan: .baton/runs/20260920-1618-pause-baton-PLAN.md
Status: complete

## Changes

Test-only round, per Director's round-02 scope (two tests and one
correction; no production code, no documents, no `VERSION`, no
`.baton/bin/`). RUN-01 is not modified; this record is append-only.

- `internal/baton/pause_test.go`: added `TestUpdateApplyRefusesWhilePaused`
  (REVIEW-01 B1) — shaped like `TestUpdateDryRunAllowedWhilePausedAndMentionsResume`
  but with `--apply`, asserting a non-zero exit whose message names the pause
  and that `AGENTS.md`, `CLAUDE.md`, and `.baton/VERSION` are byte-identical
  before and after the refused call.
- `internal/baton/pause_test.go`: reworked `TestWriteInstructionBlocksRestoresOnPartialFailure`
  to fail in the shape `instructionFiles()` actually produces — both files in
  the project root, not two separate directories — using `chflags uchg` on
  the second file to force `os.Rename` to fail onto an existing, immutable
  target while its directory stays writable. Skips (with a stated reason)
  where `chflags` is unavailable or where root would ignore the flag.
- Correction (no other file edited): RUN-01's Success Criteria line
  "`update --apply` refuses while paused and both instruction files and
  `.baton/VERSION` are unchanged... met (V3, `TestUpdateDryRunAllowedWhilePausedAndMentionsResume`)"
  is wrong. That test runs `update` without `--apply` and only asserts the
  dry-run output mentions `baton resume`; it never exercises `--apply`, so
  the `--apply` half of that criterion was untested at RUN-01, exactly as
  REVIEW-01's B1 found. `TestUpdateApplyRefusesWhilePaused`, added in this
  round, is what makes that half true.

## Validation

- Evidence collected before fixing a user-reported defect: n/a (a coverage
  gap the reviewer found via mutation testing, not a live defect: `update
  --apply` already refused correctly at RUN-01, per REVIEW-01's own
  end-to-end check).
- `go test ./...`: `ok  github.com/grollcake/baton/internal/baton`
- `go vet ./...`: clean, no output.
- `gofmt -l .`: clean, no output.
- `go run ./cmd/baton lint` (this repository): `baton-lint passed`.
- **B1 load-bearing demonstration.** Copied `internal/baton/update.go` aside,
  removed the three-line `--apply` guard (`if pausedMessage != "" { return
  errors.New(pausedMessage) }` before `preflightUpdate`), and reran
  `TestUpdateApplyRefusesWhilePaused`:
  - With the test's normal minimal upstream fixture, it failed at the
    "refusal did not mention the pause" assertion (the call instead failed
    later, inside `preflightUpdate`, on a missing upstream file — a
    consequence of the fixture being intentionally minimal once the early
    refusal is gone).
  - To see the sharper failure, I pointed the same test's `--upstream` at
    this repository's own root (a complete, valid upstream) instead of the
    minimal fixture: with the guard removed, `update --apply` completed
    successfully (`err == nil`) against a paused project, and `AGENTS.md`
    and `CLAUDE.md` came back changed (silently un-paused) while
    `.baton/VERSION` did not (same version on both sides). The test failed
    at `t.Fatal("update --apply was accepted while paused")` — the exact
    failure the guard exists to prevent. I did not leave this variant in
    place: coupling a unit test to the real repository as its upstream is
    fragile, so the committed test keeps the minimal synthetic upstream,
    which already fails without the guard (first bullet above).
  - Restored `update.go` from the saved copy, confirmed the diff against the
    RUN-01 tree was empty, and reran the full suite (`go test ./...`, `go
    vet ./...`, `gofmt -l .`) clean, and `git status` confirmed
    `internal/baton/update.go` had no uncommitted change afterward.
- **Item 2 shape correction.** Verified the new
  `TestWriteInstructionBlocksRestoresOnPartialFailure` passes on this machine
  (`chflags` present, not root): `go test ./internal/baton/ -run
  TestWriteInstructionBlocksRestoresOnPartialFailure -v` → PASS. Manually
  reproduced the underlying OS behavior first, outside Go, to confirm the
  shape is real: in a scratch directory, `chflags uchg target.txt` then `mv
  tempfile target.txt` failed with "Operation not permitted" — the same
  failure `atomicWrite`'s `os.Rename` hits. `chflags` is BSD/Darwin-only; I
  did not find a portable equivalent (Linux's `chattr +i` needs
  `CAP_LINUX_IMMUTABLE`, ordinarily root, which would test a different
  precondition than "an ordinary write fails"). The test therefore skips,
  stating why, when `chflags` is not on `PATH` or when running as root,
  rather than falling back to a shape that does not occur.
- Self smoke test after fixing a user-reported defect: n/a

## Report To Director

- Suggested summary: Round 02 — added the missing `update --apply` pause-refusal regression test, reworked the partial-write test to fail in the real single-directory shape, and recorded RUN-01's Success Criteria correction
- Artifact path: .baton/runs/20260920-1618-pause-baton-RUN-02.md

## Success Criteria Status

- B1 closed: `update --apply`'s pause refusal now has a regression test, demonstrated load-bearing by deleting the guard and observing the test fail: met
- `TestWriteInstructionBlocksRestoresOnPartialFailure` fails in the shape `instructionFiles()` actually produces (same directory), not a shape that cannot occur: met
- RUN-01's incorrect Success Criteria claim is corrected in this record without editing RUN-01: met
- No production code, documents, `VERSION`, or `.baton/bin/` changed this round: met (`git diff --stat` for this round touches only `internal/baton/pause_test.go` and adds this RUN file)

## Unresolved Risks

- N1-N5 and the wider reading of R2 from REVIEW-01 are left for Director to
  record at close, per this round's instructions; not addressed here.
- No new risk found in this round.

## Out Of Scope Returned To Director

- None.
