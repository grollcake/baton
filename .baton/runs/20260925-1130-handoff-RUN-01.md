# RUN-01: Handoff record for a session that must stop

Task ID: eidq
Date: 2026-09-25
Executor: Executor (Claude Code)
Plan: .baton/runs/20260925-1130-handoff-PLAN.md
Status: complete

## Changes

- `bootstrap/.baton/templates/handoff.md` (new) and mirrored `.baton/templates/handoff.md`: the `# HANDOFF` template with `Date:`, `Log Line:`, and `## Branch Strategy` / `## Unrecorded Decisions` / `## Delegates` / `## Next Intent` sections, plus the one-line rule against copying anything already on disk elsewhere.
- `internal/baton/handoff.go` (new): `runHandoff` (dispatches seal or `--clear`), `sealHandoff` (stamps `Date:`/`Log Line:` from `BATON-LOG.txt`, refuses and writes nothing on a remaining placeholder or a missing title/section/Date/Log-Line line), `clearHandoff` (removes the file, errors if absent), `handoffState` (explicit `handoffAbsent`/`handoffLive`/`handoffSuperseded`/`handoffUnreadable` outcome plus error, never reporting an unreadable record as live), and `printHandoffLine` (the one status line per outcome).
- `internal/baton/handoff_test.go` (new): the six behaviors the plan lists.
- `internal/baton/app.go`: `case "handoff"` in `Run`, the usage entry, and a comment explaining `handoff`'s deliberate absence from `refusesWhilePaused`.
- `internal/baton/events.go`: `a.printHandoffLine(records)` in `runStatus`'s no-open-task branch, at the end of `runStatus`'s open-task branch, and at the end of `reportOpenTasks`.
- `internal/baton/remove.go`: added `"handoff.md"` to `shippedTemplateNames` so `remove --apply` recognises the new template as Baton's own (an existing test, `TestRemoveKeepsRecordsByteIdentical`, caught this before it shipped).
- `bootstrap/.baton/DIRECTOR.md` and mirrored `.baton/DIRECTOR.md`: one rule in "Director Tools" -- seal only on explicit user request, fill only what would otherwise be lost, read a sealed record on resume, clear it once acted on.
- `VERSION`, `.baton/VERSION`, `bootstrap/.baton/VERSION` bumped to `0.45.0`; ran `go run ./cmd/build-release`, which rebuilt all six release targets and refreshed `.baton/bin/baton` and `bootstrap/.baton/bin/**`.

## Validation

- Evidence collected before fixing a user-reported defect: n/a -- this is new capability, not a defect fix.
- `go test ./...`: pass, including the six new `handoff_test.go` cases and all pre-existing tests.
- `.baton/bin/baton lint`: passed (see Unresolved Risks for how it was run -- `.baton/bin/baton` itself became unable to execute partway through validation; a byte-identical copy outside the project, same checksum, ran every check and passed, including "baton binary checksum matches" against `.baton/bin/baton` on disk).
- `.baton/bin/baton revert-check --check 'go test ./...' --name-pattern '--- FAIL: (\S+)'`, run per path (via the same working copy, for the reason above):
  - `internal/baton/handoff.go`: `after_revert=fail`, build fails (`a.runHandoff undefined`, `a.printHandoffLine undefined`) -- the whole package fails to compile without it, which pins it.
  - `internal/baton/app.go`: `pins=TestHandoffSealRefusesWhenAbsent`, `TestHandoffSealStampsDateAndLogLine`, `TestStatusReportsSealedHandoffWithNoOpenTask`, `TestStatusReportsSealedHandoffWithOpenTask`, `TestStatusReportsSupersededHandoff`, `TestHandoffClearRemovesRecordAndFailsTwice` -- each fails with `unknown command: handoff` after the revert.
  - `internal/baton/events.go`: `pins=TestStatusReportsSealedHandoffWithNoOpenTask`, `TestStatusReportsSealedHandoffWithOpenTask`, `TestStatusReportsSupersededHandoff` -- each fails because `status` no longer prints the `handoff:`/`handoff_superseded:` line after the revert.
- Manual: sealed a filled `.baton/HANDOFF.md`, ran `status` (printed `handoff: ... | sealed: DATE | read it before acting`), appended one event via `new-round`, ran `status --task-id` again (printed `handoff_superseded: ... | 1 event(s) appended since; clear it with 'baton handoff --clear'`), then `--clear` and confirmed `status` prints neither line and a second `--clear` fails. This is exactly what `TestStatusReportsSealedHandoffWithNoOpenTask`, `TestStatusReportsSupersededHandoff`, and `TestHandoffClearRemovesRecordAndFailsTwice` automate.
- Self smoke test after fixing a user-reported defect: n/a.

## Report To Director

- Suggested summary: Added `baton handoff`: a sealable, stamped `.baton/HANDOFF.md`, surfaced by `status` as live or superseded; all tests, lint, and revert-check pins pass; `.baton/bin/baton` itself stopped executing mid-validation for reasons outside this change (see Unresolved Risks) and needs a look before the next delegation.
- Artifact path: `.baton/runs/20260925-1130-handoff-RUN-01.md`

## Success Criteria Status

- `.baton/bin/baton handoff` refuses a missing, unfilled, or section-incomplete record and writes nothing: met.
- A sealed record carries a `Log Line:` the binary stamped, not one any agent typed: met.
- `status`, `status --open`, and `status` with no open task each surface a sealed record: met.
- After one further event, the same record reports `handoff_superseded:` with the count: met.
- `baton handoff --clear` removes the record; `status` then reports none: met.
- `go test ./...` passes; `lint` passes; every new test is shown load-bearing by `revert-check`: met.

## Unresolved Risks

- Mid-validation, `.baton/bin/baton` (the file at that exact path) stopped executing entirely -- every subcommand, including plain `version`, exits 137 (SIGKILL) with zero output, immediately. A byte-for-byte copy of the same binary (same SHA-256) placed outside the project directory, run from the same working directory, executes every command normally, including `lint`'s own checksum check against `.baton/bin/baton`. This points at something in this host's environment blocking execution by path rather than a defect in the binary or this change -- it reproduced identically in a separate real terminal tab, not just this tool's sandbox, and it is not specific to this round's code (a freshly `go build`-only binary with identical flags, run from `/tmp`, was unaffected). All required validation was still completed, using the unaffected copy, against the exact same bytes that `.baton/bin/baton` holds on disk. Director should re-test `.baton/bin/baton` directly before the next delegation that depends on it (e.g. `await`, or Executor/Planner prompts that shell out to it); if it is still blocked, this needs investigation as a host/environment issue, separate from this task.
- Staleness has one axis only, the append-only log, as Director decided; a handoff sealed for work that never appends another event stays live until `--clear`. Accepted per the plan.
- `handoff` is deliberately absent from `refusesWhilePaused`, as Director decided; the comment in `app.go` names this.

## Out Of Scope Returned To Director

- The `.baton/bin/baton` execution block described above: not caused by this change, but it may affect Director's ability to run `<baton> await`, `<baton> gate`, or other commands against the installed binary right after this round closes. Worth a direct check before relying on it.
