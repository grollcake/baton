# RUN-02: Handoff record for a session that must stop

Task ID: eidq
Date: 2026-09-25
Executor: Executor (Claude Code)
Plan: .baton/runs/20260925-1130-handoff-PLAN.md
Status: complete

## Changes

- `internal/baton/handoff.go`: `sealHandoff` now stamps the `## Delegates` section from what is on disk, exactly like `Date:` and `Log Line:`. New `stampDelegatesSection` refuses, writing nothing, if the section holds anything other than the template's unfilled placeholder or a body this binary already stamped (`handoffDelegatesMarker`, a bracket marker rather than `<...>` so it never trips `placeholderPattern` itself); otherwise it overwrites the section on every seal, including a re-seal of an already-superseded record (now live again, documented in both `sealHandoff` and `DIRECTOR.md`). New `delegatesStampedBody` builds one line per open task -- task id, key, last event, next gate, expected artifact and its state -- from `openTaskList` (pause.go), `nextGateDescription`, and `expectedArtifact` (both new, in events.go). New `delegateArtifactState` reports `missing`, `checkpoint` (`Status: checkpoint`), `complete, not appended` (passes `CheckArtifact`), or `incomplete`. The negative-`eventsSince` clamp in `handoffState` now carries the comment REVIEW-01 asked for.
- `internal/baton/events.go`: extracted `nextGateDescription` (the next-gate text plus an unreadable-REVIEW path) and `expectedArtifact` (the event and path a task's current stage is expected to produce next) out of `runStatus`'s inline logic; `runStatus` and `nextCommand` were refactored to call them instead of duplicating the computation, and `handoff.go` reuses both. Behaviour is unchanged -- this is the reuse the plan asked for, proven by the full existing test suite still passing untouched.
- `internal/baton/remove.go`: added `handoffFile` to the kept-paths name list (REVIEW-01 nit 1) so `HANDOFF.md` is named in `remove`'s `Keep:` report like `CONCURRENCY.md` already is; nothing was ever deleted, it was just unnamed.
- `internal/baton/remove_test.go`: new `TestRemoveReportsSealedHandoffAsKept` pins the nit-1 fix.
- `internal/baton/handoff_test.go`: `filledHandoff()`'s `## Delegates` fixture now uses the template's real (binary-owned) placeholder; rewrote `TestHandoffSealStampsDateAndLogLine` to check the sections Director still owns instead of a whole-file line diff; added `TestHandoffStampsNoOpenTasks`, `TestHandoffStampsDelegatesForOpenTask` (covers all three artifact states across a PLAN->RUN progression), `TestHandoffSealRefusesHandWrittenDelegates`, and `TestHandoffResealSupersededBecomesLiveAgain` (nit 2).
- `bootstrap/.baton/templates/handoff.md` and mirrored `.baton/templates/handoff.md`: `## Delegates` is now a single unfilled-placeholder line saying the binary fills it in; Director no longer types delegate facts there.
- `bootstrap/.baton/DIRECTOR.md` and mirrored `.baton/DIRECTOR.md`: reworded the handoff rule to state that `<baton> handoff` stamps Delegates itself and that section must not be hand-edited; kept to the plan's minimum, no other protocol prose touched.
- `VERSION`, `.baton/VERSION`, `bootstrap/.baton/VERSION` bumped to `0.46.0`; ran `go run ./cmd/build-release`, refreshing all six release targets and `.baton/bin/baton`.

## Validation

- Evidence collected before fixing a user-reported defect: n/a -- this round addresses user feedback and REVIEW-01 nits, not a reported defect.
- `go test ./...`: pass, including all new and updated tests.
- `.baton/bin/baton lint`: the exit-137 condition RUN-01 flagged as a risk recurred on `.baton/bin/baton` itself partway through this round too (every subcommand killed instantly, reproduced with a fresh rebuild). As in RUN-01, a byte-identical copy (same SHA-256) run from outside the project passed every check, including the binary-checksum check against `.baton/bin/baton` on disk: `baton-lint passed`.
- `.baton/bin/baton revert-check --check 'go test ./...' --name-pattern '--- FAIL: (\S+)'`, run per path via the same working copy:
  - `internal/baton/handoff.go`: `after_revert=fail`, build fails (`a.runHandoff`, `a.printHandoffLine` undefined; `internal/baton/remove.go:200` `undefined: handoffFile`) -- the whole package fails to compile without it.
  - `internal/baton/events.go`: `after_revert=fail`, build fails (`a.nextGateDescription`, `a.expectedArtifact` undefined in `handoff.go`) -- pins the extraction as genuinely shared, not decorative.
  - `internal/baton/remove.go`: `pins=TestRemoveKeepsRecordsByteIdentical`, `pins=TestRemoveReportsSealedHandoffAsKept` -- each fails for the reason its change was made.
- Manual: sealed a record with one open task at REQUEST -- Delegates showed `<taskID> (key <key>): last REQUEST, next PLANNED (delegate Planner), artifact <PLAN path>: missing`; advanced it to a checkpoint RUN and a complete-but-unappended RUN and resealed each time, watching the artifact state track `checkpoint` then `complete, not appended`; hand-edited the Delegates section and confirmed seal refused and left the file byte-identical; superseded a sealed record with `new-round`, confirmed `status` read `handoff_superseded:`, resealed, and confirmed it read `handoff:` (live) again with the Delegates block refreshed.
- Self smoke test after fixing a user-reported defect: n/a.

## Report To Director

- Suggested summary: `baton handoff` now stamps Delegates itself (task id, key, last event, next gate, expected artifact and its state) for every open task, refusing hand-written values there; fixed the three REVIEW-01 nits (HANDOFF.md named in remove's kept list, re-seal-of-superseded documented and pinned, eventsSince clamp commented).
- Artifact path: `.baton/runs/20260925-1130-handoff-RUN-02.md`

## Success Criteria Status

- `handoff` writes a stamped delegate-state block for every open task (task id, key, last event, next gate, expected artifact with state missing/checkpoint/complete-not-appended), reusing status/gate/check-artifact logic: met.
- The stamped block is binary-owned like `Date:`/`Log Line:`: refuses a hand-written value, overwrites on every seal, `## Delegates` in the template is now that stamped block, Director-owned sections unchanged: met.
- Three REVIEW-01 nits fixed (HANDOFF.md named in remove's kept list; re-seal-of-superseded documented and test-pinned; eventsSince clamp commented): met.
- Tests for every behaviour, each pinned by `revert-check`; `go test ./...`, `lint` pass; VERSION 0.46.0: met.

## Unresolved Risks

- The `.baton/bin/baton` exit-137 condition (execution blocked for that exact on-disk path, unrelated to this change -- see RUN-01) recurred again this round after every rebuild. It is not caused by this diff: a byte-identical copy outside the project runs every command normally, including the checksum check against the very file that will not run in place. This is now observed twice across two rounds in this environment and is worth a direct look by Director or the user before it blocks a real `await`/`gate` call.
- Staleness (log-only axis) and `handoff` staying out of `refusesWhilePaused` are unchanged from RUN-01, both per Director's prior decisions.
- `delegateArtifactState`'s `incomplete` bucket (a file present but neither checkpoint nor passing `CheckArtifact`) is not separately exercised by a test; it is a natural fourth state alongside the three the plan named and did not seem worth a dedicated case, but flagging it in case Director wants one.

## Out Of Scope Returned To Director

- The recurring `.baton/bin/baton` execution block, as above -- not part of this task's scope to fix, but Director should verify the binary runs directly before relying on it for the next delegation.
